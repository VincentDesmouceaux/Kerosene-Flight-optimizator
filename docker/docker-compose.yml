services:
  gui:
    build:
      context: ..
      dockerfile: Dockerfile
    image: kerosene-app:latest
    container_name: kerosene-gui
    environment:
      PYTHONUNBUFFERED: "1"
      DEBUG: "1"
      TARGET_FPS: "20"   # 👈 ralentit la cadence d’images
      SUBSTEPS: "8"      # 👈 rallonge la durée d’un cycle de vent
    ports:
      - "8080:8080"
    restart: unless-stopped
